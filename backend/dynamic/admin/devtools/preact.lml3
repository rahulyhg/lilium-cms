const settings = {
    title : "Preact integration",
    lmldom : { v : 1.0 }
};

const compile = (o, context) => { o(context.header, `

    <h3>Custom Preact integration</h3>
    <p>This paragraph is not part of a Preact DOM.</p>

    <h3>But, then...</h3>
    ${context.libs.app('devtools')}

`); };

module.exports = { settings, compile };
