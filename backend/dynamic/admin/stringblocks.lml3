const settings = {
    lmldom : {v : 1.0},
    title : "String blocks",
    livevars : [{ name : "stringblocks.list" }],
    tools : {
        "add" : { call : "newSBDialog" }
    }
};

const preload = (context, done) => {
    context.script = `<script>
        
    </script>`;

    done();
};

const compile = (o, context) => {
    o(context.header);
};

module.exports = { settings, preload, compile };
