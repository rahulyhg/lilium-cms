{#config;theme;extra}
{$ if (extra.contextname == "home"); $}
<title>{=config.default.info.project}</title>
{$ endif; if (extra.contextname == "article"); $}
<title>{=extra.article.title} - {=config.default.info.project}</title>
{$ endif; $}
